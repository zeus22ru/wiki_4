# Полный план улучшений проекта Wiki QA

## 📋 Обзор

Документ содержит детальный план внедрения всех улучшений для вопрос-ответной системы на базе векторной базы знаний.

---

## 🎯 Этап 1: Критические улучшения (Приоритет 🔴)

### 1.1 Централизованная конфигурация

**Файлы:**
- `config/settings.py` - основной файл конфигурации
- `.env` - переменные окружения
- `.env.example` - пример конфигурации

**Задачи:**
- [x] Создать `config/settings.py` с классом Settings
- [x] Перенести все хардкод значения из скриптов
- [x] Создать `.env.example` с описанием всех переменных
- [x] Обновить `requirements.txt` добавить `python-dotenv`
- [x] Обновить все скрипты для использования конфигурации

**Переменные окружения:**
```env
# Ollama
OLLAMA_URL=http://localhost:11434
OLLAMA_EMBEDDING_MODEL=bge-m3
OLLAMA_CHAT_MODEL=qwen2.5:7b

# ChromaDB
CHROMA_PERSIST_DIR=./chroma_db
CHROMA_COLLECTION_NAME=wiki_knowledge

# Data
DATA_DIR=./data
CHUNK_SIZE=500
CHUNK_OVERLAP=50
BATCH_SIZE=10

# API
API_HOST=0.0.0.0
API_PORT=5000
TOP_K_RESULTS=3

# Logging
LOG_LEVEL=INFO
LOG_DIR=./logs
```

---

### 1.2 Структурированное логирование

**Файлы:**
- `config/logging_config.py` - конфигурация логирования
- `logs/` - директория для логов

**Задачи:**
- [x] Создать `config/logging_config.py`
- [x] Настроить ротацию логов (RotatingFileHandler)
- [x] Добавить логирование в `create_vector_db.py`
- [x] Добавить логирование в `qa_system.py`
- [x] Добавить логирование в `web_app.py`
- [x] Добавить логирование запросов и ответов API

**Уровни логирования:**
- DEBUG: детальная информация для отладки
- INFO: информационные сообщения
- WARNING: предупреждения
- ERROR: ошибки
- CRITICAL: критические ошибки

---

### 1.4 Markdown рендеринг

**Файлы:**
- `static/js/markdown.js` - библиотека для рендеринга
- `static/css/markdown.css` - стили для Markdown

**Задачи:**
- [x] Добавить библиотеку `marked.js` или `markdown-it`
- [x] Добавить библиотеку `highlight.js` для подсветки кода
- [x] Обновить `formatMessage()` в `script.js`
- [x] Создать стили для Markdown элементов
- [x] Добавить поддержку таблиц, списков, кода

---

### 1.6 Валидация входных данных

**Файлы:**
- `utils/validators.py` - валидаторы
- `api/middleware/validation.py` - middleware валидации

**Задачи:**
- [x] Создать валидаторы для:
  - Сообщений чата (длина, запрещённые символы)
  - Параметров запросов
- [x] Добавить санитизацию HTML (bleach)
- [x] Реализовать middleware для валидации
- [x] Добавить `requirements.txt`: `bleach`, `pydantic`

---
### 1.9 RAG с цитированием

**Файлы:**
- `core/rag.py` - RAG логика
- `utils/formatters.py` - форматирование ответов

**Задачи:**
- [x] Реализовать извлечение цитат из документов
- [x] Добавить метаданные с позициями цитат
- [x] Обновить prompt для включения цитат
- [x] Форматировать ответ с подсветкой цитат
- [x] Добавить ссылки на источники в ответе

---

### 1.10 Кэширование эмбеддингов

**Файлы:**
- `utils/cache.py` - кэширование
- `core/embeddings.py` - работа с эмбеддингами

**Задачи:**
- [x] Создать кэш для эмбеддингов запросов (Redis или файловый)
- [x] Реализовать TTL для кэша
- [x] Добавить кэширование в `get_embedding()`
- [x] Добавить инвалидацию кэша при обновлении документов
- [x] Добавить метрики hit/miss ratio

---

## 🎯 Этап 2: Важные улучшения (Приоритет 🟡)

### 2.1 Модульная структура

**Задачи:**
- [ ] Создать структуру директорий
- [ ] Перенести код в модули
- [ ] Создать `__init__.py` файлы
- [ ] Обновить импорты во всех файлах
- [ ] Создать `main.py` как точку входа

---

### 2.2 Асинхронная обработка

**Фадлы:**
- `main.py` - async Flask (Quart или FastAPI)

**Задачи:**
- [ ] Перейти на FastAPI или Quart
- [ ] Переписать все функции как async
- [ ] Использовать `aiohttp` вместо `requests`
- [ ] Добавить `asyncio` для параллельной обработки
- [ ] Обновить `requirements.txt`: `fastapi`, `uvicorn`, `httpx`

---

### 2.3 Гибридный поиск

**Файлы:**
- `core/hybrid_search.py` - гибридный поиск

**Задачи:**
- [ ] Добавить BM25 полнотекстовый поиск
- [ ] Реализовать fusion векторного и полнотекстового поиска
- [ ] Добавить веса для разных типов поиска
- [ ] Добавить `requirements.txt`: `rank-bm25`
- [ ] Настроить параметры гибридного поиска

---

### 2.4 Re-ranking

**Файлы:**
- `core/reranker.py` - переранжирование

**Задачи:**
- [ ] Реализовать cross-encoder для переранжирования
- [ ] Добавить фильтрацию по релевантности
- [ ] Настроить порог отсечки
- [ ] Добавить кэширование результатов переранжирования

---

### 3.1 Тёмная тема ✅

**Файлы:**
- [`static/css/theme.css`](static/css/theme.css:1) - стили тем
- [`static/js/theme.js`](static/js/theme.js:1) - переключение тем

**Задачи:**
- [x] Создать CSS переменные для тем
- [x] Реализовать переключатель тем
- [x] Сохранять предпочтение в localStorage
- [x] Добавить системную тему (auto)

---

### 3.2 Копирование ответов ✅

**Файлы:**
- [`static/js/clipboard.js`](static/js/clipboard.js:1) - копирование

**Задачи:**
- [x] Добавить кнопку копирования к каждому сообщению
- [x] Реализовать копирование в буфер обмена
- [x] Добавить уведомление о копировании
- [x] Добавить `requirements.txt`: `clipboard.js`

## 📊 Общий прогресс

| Этап | Всего задач | Выполнено | В прогрессе |
|------|-------------|-----------|-------------|
| Этап 1 (Критические) | 6 | 6 | 0 |
| Этап 2 (Важные) | 4 | 0 | 0 |
| Этап 3 (UX) | 2 | 2 | 0 |
| **Итого** | **12** | **8** | **0** |

---

## ✅ Выполненные задачи Этапа 1

### 1.1 Централизованная конфигурация ✅
- Создан [`config/settings.py`](config/settings.py:1) с классом Settings
- Все хардкод значения перенесены в конфигурацию
- Создан [`.env.example`](.env.example:1) с описанием всех переменных
- Обновлён [`requirements.txt`](requirements.txt:1) с добавлением `python-dotenv`
- Все скрипты обновлены для использования конфигурации

### 1.2 Структурированное логирование ✅
- Создан [`config/logging_config.py`](config/logging_config.py:1)
- Настроена ротация логов (RotatingFileHandler)
- Логирование добавлено в [`create_vector_db.py`](create_vector_db.py:1)
- Логирование добавлено в [`qa_system.py`](qa_system.py:1)
- Логирование добавлено в [`web_app.py`](web_app.py:1)
- Логирование запросов и ответов API реализовано

### 1.4 Markdown рендеринг ✅
- Добавлена библиотека `marked.js` в [`templates/index.html`](templates/index.html:1)
- Добавлена библиотека `highlight.js` для подсветки кода
- Обновлена [`formatMessage()`](static/script.js:143) в [`script.js`](static/script.js:1)
- Созданы стили для Markdown элементов в [`static/css/markdown.css`](static/css/markdown.css:1)
- Добавлена поддержка таблиц, списков, кода

### 1.6 Валидация входных данных ✅
- Созданы валидаторы в [`utils/validators.py`](utils/validators.py:1)
- Добавлена санитизация HTML (bleach)
- Реализовано middleware для валидации в [`api/middleware/validation.py`](api/middleware/validation.py:1)
- Обновлён [`requirements.txt`](requirements.txt:1) с добавлением `bleach`, `pydantic`

### 1.9 RAG с цитированием ✅
- Реализовано извлечение цитат из документов в [`core/rag.py`](core/rag.py:1)
- Добавлены метаданные с позициями цитат
- Обновлён prompt для включения цитат
- Реализовано форматирование ответа с подсветкой цитат
- Добавлены ссылки на источники в ответе
- Созданы форматировщики в [`utils/formatters.py`](utils/formatters.py:1)

### 1.10 Кэширование эмбеддингов ✅
- Создан файловый кэш для эмбеддингов в [`utils/cache.py`](utils/cache.py:1)
- Реализован TTL для кэша
- Кэширование добавлено в [`get_embedding()`](qa_system.py:20) в [`qa_system.py`](qa_system.py:1)
- Кэширование добавлено в [`get_embedding()`](create_vector_db.py:75) и [`get_embeddings_batch()`](create_vector_db.py:99) в [`create_vector_db.py`](create_vector_db.py:1)
- Добавлена инвалидация кэша при обновлении документов
- Добавлены метрики hit/miss ratio

---

## ✅ Выполненные задачи Этапа 3

### 3.1 Тёмная тема ✅
- Создан [`static/css/theme.css`](static/css/theme.css:1) с CSS переменными для светлой и тёмной темы
- Создан [`static/js/theme.js`](static/js/theme.js:1) с классом ThemeManager для управления темами
- Реализован переключатель тем (light → dark → auto)
- Добавлено сохранение предпочтения в localStorage
- Добавлена поддержка системной темы (auto)
- Переключатель тем добавлен в [`templates/index.html`](templates/index.html:1)
- Подключён файл стилей тем в HTML

### 3.2 Копирование ответов ✅
- Создан [`static/js/clipboard.js`](static/js/clipboard.js:1) с классом ClipboardManager
- Добавлена кнопка копирования к каждому сообщению
- Реализовано копирование текста в буфер обмена
- Добавлено уведомление (toast) о копировании
- Кнопки копирования автоматически добавляются к новым сообщениям через MutationObserver
- Подключён файл clipboard.js в [`templates/index.html`](templates/index.html:1)

---

## 🚀 Порядок реализации

Рекомендуется внедрять улучшения в следующем порядке:

1. **Инфраструктура:** Конфигурация → Логирование → Модульная структура
2. **Безопасность:** Валидация
3. **Функциональность:** RAG с цитированием → Кэширование
4. **UI/UX:** Markdown → Тёмная тема → Копирование
5. **Производительность:** Асинхронность → Гибридный поиск → Re-ranking
6. **Интеграции:** Telegram/Slack

---

## 📝 Заметки

- Все изменения должны сопровождаться тестами
- Каждое улучшение должно быть задокументировано
- Использовать git branches для каждой задачи
- Проводить code review перед мержем
