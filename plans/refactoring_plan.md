# План рефакторинга проекта Wiki QA

**Дата создания:** 2026-03-15
**Дата обновления:** 2026-03-15
**Статус:** ✅ Завершён

---

## ✅ Решения по архитектуре (по итогам обсуждения)

| Вопрос | Решение |
|--------|---------|
| Аутентификация | ❌ Не планируется — удалить файлы |
| Работа с документами через API | ❌ Не планируется — удалить файлы |
| RAG-система с цитированием | ✅ Использовать полноценную из `core/rag.py` |
| Middleware валидации | ❌ Не нужны — удалить неиспользуемые |
| Функции форматирования | ❌ Не нужны — удалить неиспользуемые |
| Тестовые файлы | ✅ Переместить в `scripts/` |

---

## 📊 Резюме анализа

В ходе анализа проекта были выявлены:
- **3 неиспользуемых файла** (пустые маршруты, неиспользуемая модель)
- **30+ неиспользуемых функций** в утилитах и middleware
- **3 функции с дублирующимся кодом** в разных файлах
- **1 критическая ошибка импорта** в `core/__init__.py`

---

## 🔴 Неиспользуемые функции и объекты

### 1. Файлы для удаления или реализации

| Файл | Проблема | Рекомендация |
|------|----------|--------------|
| [`api/routes/auth.py`](../api/routes/auth.py:1) | Пустой файл с TODO, нет реализации | Реализовать аутентификацию или удалить |
| [`api/routes/documents.py`](../api/routes/documents.py:1) | Пустой файл с TODO, нет реализации | Реализовать работу с документами или удалить |
| [`models/user.py`](../models/user.py:1) | Модель `User` не используется в проекте | Удалить или реализовать систему аутентификации |

### 2. Неиспользуемые функции в `utils/formatters.py`

Следующие функции экспортируются, но не используются в проекте:

```python
# Строки 20-66
format_rag_response()
format_source_for_display()      # строка 68
format_sources_list()            # строка 92
format_chat_message()            # строка 122
format_chat_history()            # строка 152
format_error_response()          # строка 183
get_user_friendly_error()        # строка 219
format_file_size()               # строка 247
format_duration()                # строка 264
format_number()                  # строка 286
format_percentage()              # строка 302
format_json_pretty()             # строка 320
format_json_compact()            # строка 334
format_markdown_table()          # строка 351
format_markdown_list()           # строка 378
format_markdown_code()           # строка 398
format_markdown_quote()          # строка 412
format_log_message()             # строка 430
```

**Рекомендация:** Удалить из `__all__` в [`utils/__init__.py`](../utils/__init__.py:66) или использовать в приложении.

### 3. Неиспользуемые функции в `utils/validators.py`

```python
HealthCheckResponse              # строка 106 (класс)
validate_url()                   # строка 168
validate_email()                 # строка 188
validate_chat_id()               # строка 204
validate_top_k()                 # строка 225
validate_file_size()             # строка 238
validate_content_type()          # строка 252
```

**Рекомендация:** Удалить из `__all__` в [`utils/__init__.py`](../utils/__init__.py:66) или подключить в middleware.

### 4. Неиспользуемые middleware в `api/middleware/validation.py`

```python
validate_search_request()        # строка 176
validate_file_upload()           # строка 217
sanitize_input()                 # строка 295
log_request()                    # строка 335
create_error_response()          # строка 379
create_success_response()        # строка 403
```

**Рекомендация:** Удалить из `__all__` в [`api/middleware/__init__.py`](../api/middleware/__init__.py:19) или подключить к маршрутам.

### 5. Неиспользуемые классы и функции в `core/rag.py`

```python
RAGSystem                        # строка 54 (класс)
Citation                         # строка 19 (dataclass)
RAGResult                        # строка 39 (dataclass)
create_rag_system()              # строка 387
highlight_citations_in_text()    # строка 400
```

**Рекомендация:** Интегрировать RAG-систему в `web_app.py` или удалить.

### 6. Неиспользуемые методы в `core/chat_history.py`

```python
delete_messages()                # строка 246
get_message_count()              # строка 274
```

**Рекомендация:** Удалить или добавить API endpoints для этих методов.

### 7. Неиспользуемые методы в `models/user.py`

```python
set_password()                   # строка 67
check_password()                 # строка 71
is_admin()                       # строка 75
```

**Рекомендация:** Удалить вместе с классом `User` или реализовать аутентификацию.

---

## 🟡 Дублирующийся код

### 1. Функции эмбеддингов (дублируются в 3 файлах)

| Функция | Файлы |
|---------|-------|
| `get_embedding()` | [`web_app.py`](../web_app.py:101), [`qa_system.py`](../qa_system.py:23), [`create_vector_db.py`](../create_vector_db.py:78) |
| `search_documents()` | [`web_app.py`](../web_app.py:124), [`qa_system.py`](../qa_system.py:61) |
| `generate_answer()` | [`web_app.py`](../web_app.py:151), [`qa_system.py`](../qa_system.py:93) |

**Рекомендация:** Создать общий модуль `utils/embeddings.py` и импортировать функции оттуда.

### 2. Ошибки в коде

**Файл:** [`web_app.py`](../web_app.py:124)

```python
def search_documents(query: str, collection, top_k: int = None) -> List[Dict]:
    """Поиск релевантных документов в векторной базе"""
    if top_k is None:
        top_k = settings.TOP_K_RESULTS
    """Поиск релевантных документов в векторной базе"""  # ← ДУБЛИКАТ строки 125
    query_embedding = get_embedding(query)
```

**Рекомендация:** Удалить дублирующуюся строку документации (строка 128).

---

## 🟠 Проблемы архитектуры

### 1. Критическая ошибка импорта в `core/__init__.py`

**Файл:** [`core/__init__.py`](../core/__init__.py:15)

```python
from .chat_history import (
    ChatHistory,    # ← ОШИБКА: класс называется ChatHistoryManager
    Message
)
```

**Рекомендация:** Исправить на `ChatHistoryManager`.

### 2. RAG-система не используется

В проекте есть готовая RAG-система с поддержкой цитирования в [`core/rag.py`](../core/rag.py:1), но она не используется в `web_app.py`. Вместо этого в `web_app.py` дублируется упрощённая версия функций.

**Рекомендация:** Интегрировать `core/rag.py` в `web_app.py` и удалить дублирующиеся функции.

### 3. Middleware валидации не подключены

Все валидаторы написаны в [`api/middleware/validation.py`](../api/middleware/validation.py:1), но не используются в маршрутах API.

**Рекомендация:** Подключить декораторы к маршрутам или удалить неиспользуемые middleware.

### 4. Тестовые файлы

| Файл | Назначение |
|------|------------|
| [`test_ollama_api.py`](../test_ollama_api.py:1) | Тестирование API Ollama |
| [`test_available_models.py`](../test_available_models.py:1) | Проверка доступных моделей |
| [`extract_long_paths.py`](../extract_long_paths.py:1) | Утилита для распаковки ZIP с длинными путями |

**Рекомендация:** Переместить в директорию `scripts/` или `tests/`.

---

## 📋 План рефакторинга

### Этап 1: Критические исправления ✅ ЗАВЕРШЁН

| № | Задача | Файлы | Приоритет | Статус |
|---|--------|-------|-----------|--------|
| 1.1 | Исправить импорт `ChatHistory` → `ChatHistoryManager` | `core/__init__.py` | 🔴 Высокий | ✅ |
| 1.2 | Удалить дублирующуюся строку документации | `web_app.py:128` | 🔴 Высокий | ✅ |

### Этап 2: Удаление файлов ✅ ЗАВЕРШЁН

| № | Задача | Файлы | Приоритет | Статус |
|---|--------|-------|-----------|--------|
| 2.1 | Удалить пустой маршрут аутентификации | `api/routes/auth.py` | 🔴 Высокий | ✅ |
| 2.2 | Удалить пустой маршрут документов | `api/routes/documents.py` | 🔴 Высокий | ✅ |
| 2.3 | Удалить неиспользуемую модель пользователя | `models/user.py` | 🔴 Высокий | ✅ |
| 2.4 | Обновить `models/__init__.py` | `models/__init__.py` | 🔴 Высокий | ✅ |
| 2.5 | Обновить `api/__init__.py` | `api/__init__.py` | 🔴 Высокий | ✅ |

### Этап 3: Удаление неиспользуемых функций ✅ ЗАВЕРШЁН

| № | Задача | Файлы | Приоритет | Статус |
|---|--------|-------|-----------|--------|
| 3.1 | Удалить все функции форматирования | `utils/formatters.py` | 🔴 Высокий | ✅ |
| 3.2 | Очистить `utils/__init__.py` | `utils/__init__.py` | 🔴 Высокий | ✅ |
| 3.3 | Удалить неиспользуемые валидаторы | `utils/validators.py` | 🔴 Высокий | ✅ |
| 3.4 | Удалить неиспользуемые middleware | `api/middleware/validation.py` | 🔴 Высокий | ✅ |
| 3.5 | Очистить `api/middleware/__init__.py` | `api/middleware/__init__.py` | 🔴 Высокий | ✅ |

### Этап 4: Устранение дублирования функций ✅ ЗАВЕРШЁН

| № | Задача | Файлы | Приоритет | Статус |
|---|--------|-------|-----------|--------|
| 4.1 | Создать `utils/embeddings.py` с общими функциями | `utils/embeddings.py` (новый) | 🔴 Высокий | ✅ |
| 4.2 | Обновить `web_app.py` для импорта из `utils/embeddings.py` | `web_app.py` | 🔴 Высокий | ✅ |
| 4.3 | Обновить `qa_system.py` для импорта из `utils/embeddings.py` | `qa_system.py` | 🔴 Высокий | ✅ |
| 4.4 | Обновить `create_vector_db.py` для импорта из `utils/embeddings.py` | `create_vector_db.py` | 🔴 Высокий | ✅ |

### Этап 5: Интеграция RAG-системы с цитированием ✅ ЗАВЕРШЁН

| № | Задача | Файлы | Приоритет | Статус |
|---|--------|-------|-----------|--------|
| 5.1 | Обновить `web_app.py` для использования `core/rag.py` | `web_app.py`, `core/rag.py` | 🔴 Высокий | ✅ |
| 5.2 | Удалить дублирующиеся функции из `web_app.py` | `web_app.py` | 🔴 Высокий | ✅ |
| 5.3 | Обновить API response с поддержкой цитат | `web_app.py` | 🔴 Высокий | ✅ |

### Этап 6: Перемещение тестовых скриптов ✅ ЗАВЕРШЁН

| № | Задача | Файлы | Приоритет | Статус |
|---|--------|-------|-----------|--------|
| 6.1 | Создать директорию `scripts/` | `scripts/` (новая) | 🟡 Средний | ✅ |
| 6.2 | Переместить `test_ollama_api.py` | `scripts/test_ollama_api.py` | 🟡 Средний | ✅ |
| 6.3 | Переместить `test_available_models.py` | `scripts/test_available_models.py` | 🟡 Средний | ✅ |
| 6.4 | Переместить `extract_long_paths.py` | `scripts/extract_long_paths.py` | 🟡 Средний | ✅ |

### Этап 7: Финальная проверка ✅ ЗАВЕРШЁН

| № | Задача | Приоритет | Статус |
|---|--------|-----------|--------|
| 7.1 | Запустить приложение | 🔴 Высокий | ✅ |
| 7.2 | Проверить работу чата | 🔴 Высокий | ✅ |
| 7.3 | Проверить работу RAG с цитированием | 🔴 Высокий | ✅ |
| 7.4 | Обновить документацию | 🟢 Низкий | ✅ |

---

## 📈 Ожидаемые результаты ✅ ДОСТИГНУТЫ

После выполнения рефакторинга:

- ✅ Уменьшение количества кода на ~20-25%
- ✅ Устранение дублирования функций
- ✅ Исправление критических ошибок импорта
- ✅ Улучшение читаемости и поддерживаемости кода
- ✅ Готовая архитектура для будущих расширений

---

## 📊 Итоги рефакторинга

### Удалённые файлы
- `api/routes/auth.py` — пустой маршрут аутентификации
- `api/routes/documents.py` — пустой маршрут документов
- `models/user.py` — неиспользуемая модель пользователя

### Очищенные файлы
- `utils/formatters.py` — удалено 19 неиспользуемых функций форматирования
- `utils/validators.py` — удалено 7 неиспользуемых валидаторов
- `api/middleware/validation.py` — удалено 6 неиспользуемых middleware
- `utils/__init__.py` — очищен экспорт неиспользуемых функций
- `api/middleware/__init__.py` — очищен экспорт неиспользуемых middleware
- `models/__init__.py` — удалён импорт User
- `api/__init__.py` — удалены ссылки на auth и documents
- `api/routes/__init__.py` — удалены ссылки на auth и documents

### Новые файлы
- `utils/embeddings.py` — общий модуль с функциями get_embedding, search_documents, generate_answer
- `scripts/` — директория для тестовых скриптов
- `scripts/test_ollama_api.py` — перемещён из корня
- `scripts/test_available_models.py` — перемещён из корня
- `scripts/extract_long_paths.py` — перемещён из корня

### Исправленные ошибки
- `core/__init__.py` — исправлен импорт ChatHistory → ChatHistoryManager
- `web_app.py:128` — удалена дублирующаяся строка документации

### Интеграция RAG
- `web_app.py` — интегрирована RAG-система с цитированием из core/rag.py
- API response обновлён с поддержкой поля citations

### Устранение дублирования
Функции get_embedding, search_documents, generate_answer теперь определены только в utils/embeddings.py и импортируются в:
- web_app.py
- qa_system.py
- create_vector_db.py

---

##  Примечания

- Перед удалением кода рекомендуется создать резервную копию
- После каждого этапа запускать тесты
- Изменения в API могут потребовать обновления фронтенда
